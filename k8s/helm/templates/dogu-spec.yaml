apiVersion: v1
kind: ConfigMap
metadata:
  name: component-spec-{{ include "cas.fullname" . }}
  labels: {{ include "cas.labels" . | nindent 4 }}
data:
  current: "{{ .Chart.AppVersion }}"
  "{{ .Chart.AppVersion }}": |
    {{ .Files.Get "dogu.json" | nindent 4 }}