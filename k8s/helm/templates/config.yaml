apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "cas.fullname" . }}-config
  labels:
    {{- include "cas.labels" . | nindent 4 }}
    {{- include "cas.backupLabels" . | nindent 4 }}
data:
  normal: |
    {{ toYaml .Values.configuration.normal | nindent 4 }}
  sensitive: |
    {}